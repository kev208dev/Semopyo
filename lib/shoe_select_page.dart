import 'package:flutter/material.dart';

class ShoeSelectPage extends StatelessWidget {
  const ShoeSelectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          
        ],
      )
    );
  }
}
